package com.exemple.adapter.backapp.Repository;

import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.util.List;
import java.util.Optional;

/**
 * Repositório para testes
 */
//@Repository
//public interface TesteRepository extends JpaRepository<TesteModel, Long> {
//
//    Optional<TesteModel> findByNome(String nome);
//
//    @Query("SELECT t FROM TesteModel t WHERE t.nome LIKE %:nome%")
//    List<TesteModel> buscarPorNome(@Param("nome") String nome);
//}
//
